<!doctype html>
<html>
  <head>
    <title>{{#if page.title}}{{ page.title }} | {{/if}}{{ site.name }}</title>
    {{#if page.meta_description }}<meta name="description" content="{{ page.meta_description }}">{{/if}}
    {{#if site.webmention_endpoint}}<link href="{{ site.webmention_endpoint }}" rel="webmention">{{/if}}
    <link rel="stylesheet" href="{{ site.baseURL }}/site.css">
    <script src="{{ site.baseURL }}/site.js"></script>
  </head>

  <body id="{{ page.fullPath }}{{#unless page.fullPath}}home{{/unless}}"
        class="{{#each page.path}} {{ this }}{{/each}}">
        
    <header>
      {{#if site.name}}
      <h1 class="title"><a href="{{ site.baseURL }}">{{ site.name}}</a></h1>
      {{/if}}
      <p class="description">{{ site.description }}</p>

      {{#if site.author }}
      <section class="author h-card">
        {{#with site.author }}
          <h3 class="p-name">{{ name }}</h3>
          {{#if image}}<img src="{{ image }}" class="u-photo">{{/if}}
          {{#if note}}<div class="p-note">{{ note }}</div>{{/if}}
          {{#each external_profiles}}
            <a href="{{ url }}" rel="me" class="u-url">{{ name }}</a>
          {{/each}}
        {{/with}}
      </section>
      {{/if}}

    </header>

    <main class="h-entry">
      <section class="breadcrumb">
        {{#if page.fullPaths}}
        <ul>
          {{#each page.fullPaths}}
            <li><a href="{{ ../site.baseURL }}/{{ this.full }}">{{ this.fragment }}</a></li>
          {{/each}}
        </ul>
        {{/if}}
      </section>
      <section class="text e-content">
        {{{ page.html }}}
      </section>
      <section class="data">
        {{{ page.data }}}
      </section>
      <section class="children">
        <ul>
          {{#each page.children}}
            <li>
              <a href="{{ ../site.baseURL }}/{{#each path }}{{ this }}/{{/each}}{{ slug }}">
                {{#if title}}{{title}}{{/if}}{{#unless title}}{{slug}}{{/unless}}
              </a>
              {{#if date}} - <small><time>{{ date }}</time></small>{{/if}}
            </li>
          {{/each}}
        </ul>
      </section>
      {{#if page.date}}
      <time class="dt-published dt-updated" datetime="{{ page.date }}">
        {{ page.date }}
      </time>
      {{/if}}
    </main>

    <footer></footer>

  </body>

</html>
